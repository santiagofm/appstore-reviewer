import { Typography } from 'antd';

export default function App() {
  return (
    <div style={{ padding: 24 }}>
      <Typography.Title level={2}>App Store Reviews</Typography.Title>
    </div>
  );
}
